npm-install: 
	cd home && npm install
	cd server && npm install
	cd addtocard && npm install
	cd cart && npm install
	cd pdp && npm install

yarn-install: 
	cd home && yarn install
	cd server && yarn install
	cd addtocard && yarn install
	cd cart && yarn install
	cd pdp && yarn install